import chalk from 'chalk';
export function cliDebugPrint(tag, ...args) {
    if (process.env.APIFY_CLI_DEBUG) {
        console.error(chalk.gray(`[${tag}]`), ...args);
    }
}
//# sourceMappingURL=cliDebugPrint.js.map